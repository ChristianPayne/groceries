import { Headers as NodeHeaders, Request as NodeRequest } from "@remix-run/node";
import type { Handler, HandlerEvent, HandlerContext, HandlerResponse } from "@netlify/functions";
import type { AppLoadContext, ServerBuild, Response as NodeResponse } from "@remix-run/node";
/**
 * A function that returns the value to use as `context` in route `loader` and
 * `action` functions.
 *
 * You can think of this as an escape hatch that allows you to pass
 * environment/platform-specific values through to your loader/action.
 */
export declare type GetLoadContextFunction = (event: HandlerEvent, context: HandlerContext) => AppLoadContext;
export declare type RequestHandler = Handler;
export declare function createRequestHandler({ build, getLoadContext, mode, }: {
    build: ServerBuild;
    getLoadContext?: AppLoadContext;
    mode?: string;
}): RequestHandler;
export declare function createRemixRequest(event: HandlerEvent): NodeRequest;
export declare function createRemixHeaders(requestHeaders: HandlerEvent["multiValueHeaders"]): NodeHeaders;
export declare function sendRemixResponse(nodeResponse: NodeResponse): Promise<HandlerResponse>;
